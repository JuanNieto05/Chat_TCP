[build]
builder = "nixpacks"
buildCommand = "./gradlew :server:build -x test"  # -x test para saltar tests temporalmente

[deploy]
startCommand = "cd server && java -cp build/libs/server.jar ui.Main"

[service]
port = 10000

[env]
JAVA_VERSION = "21"